# This file marks 3rdparty/zstd as a Bazel package.
# The actual build rules are in zstd.BUILD, used by http_archive("zstd_lib") in http.bzl.
# Note: the http_archive is named "zstd_lib" (not "zstd") to avoid Bazel label resolution
# conflicts with Mooncake's internal 3rdparty/zstd directory in the @mooncake build_file context.
